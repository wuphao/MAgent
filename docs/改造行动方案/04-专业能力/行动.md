# 阶段 04 行动：逐个迁移专业分析

## 04-A1 建能力注册与统一执行返回

新增 domain/capabilities.py、registries/capabilities.py、capabilities/invoker.py。
定义 ToolResult 的成功、不可用、输入不适用、技术失败；记录输入引用、工具/规则版本、耗时和产物位置。插件不得返回任意未经验证的 dict 后直接入证据仓。

先迁移合成 XX-v1 的计分与核对能力，形成“标准观测 → 计算产物 → 证据 → 发现”集成测试。

## 04-A2 迁移量表角色

新增 capabilities/assessments.py、agents/assessment.py；逐项拆分 cognitive_agent.py、functional_agent.py 的字段读取与业务计算。

MMSE/MoCA/FAQ/CDR/ADAS 逐个建立规则清单：哪些是原始报告值、哪些可重算、哪些有解释依据。未知规则只做描述，不能为了对齐旧输出补造定义。

FAQ 增加备注的夹具必须通过；CDR 总体评分、分项与 CDR-SB 使用不同概念，不互相替代。

## 04-A3 迁移纵向工具

新增 capabilities/longitudinal.py、agents/longitudinal.py。保留可验证的数学计算，重建输入分组、日期解析和不足状态。

测试乱序、同日、多版本、缺测、只有月精度、相同时间点和全常量序列。统计结果的 n 要说明是原始条数还是有效时间点，避免汇总只看原始 n。

## 04-A4 迁移检测、遗传和目标质量

新增 capabilities/laboratory.py、agents/laboratory_genetics.py、capabilities/quality.py。
从 biomarker_agent.py 抽取适用的纯解析函数；为 <、>、单位及平台建立结构化值。将 REQUIRED_FORMS 换成目标到必要能力/输入的规则表。

## 04-A5 统一 Agent 契约

新增 agents/base.py，旧 BaseAgent 不再被新角色继承。run 只接收当前快照和能力访问对象，不读 raw_case/normalized_case。保留一个临时固定执行顺序驱动器，作为阶段 05 前的可运行入口。

## 04-A6 验收

待实现命令：
```powershell
python -m pytest tests/integration/test_specialists.py tests/acceptance/test_semantic_analysis.py
```

覆盖 S10—S13，并将 S01 的四种结构经映射后输入同一 Agent，核对相同语义发现。用 rg 检查新 agents/ 是否出现来源中文字段和 normalized_case；检查结果需人工判断，概念展示名不等于违法字段依赖。

产物：规则核查表、工具契约、各专业结果、旧新差异解释。将“旧代码结论与新代码不同但新结果符合独立期望”记为预期纠错。

## 风险、顺序与回退

量表、纵向、检测分别一个 PR，避免一次改完难以定位。规则无来源、跨版本可比性未明确时限制输出，不用旧推断填空。

回退按能力版本和运行入口进行。已经被新协议发布的证据不能交给旧 synthesis_agent 直接拼接；旧系统应独立完成自己的整次运行。

